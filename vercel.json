{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "framework": "react",
  "outputDirectory": "dist",
  "env": {
    "GEMINI_API_KEY": "@gemini_api_key"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
